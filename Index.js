// --- Core Game Logic ---
let houseBalance = 1000.0;

// Blackjack
function dealCards() {
  // ... (enhanced card dealing with animations)
}

// Slots 
const cosmicSymbols = ['🌌', '🪐', '☄️', '🌠', '✨', '👽']; // New cosmic theme

function spinSlots() {
  // ... (warp-speed spin animation)
}

// Plinko
function dropBall() {
  // ... (glowing trail physics)
}

// --- Utility Functions ---
function updateBalance() {
  document.getElementById('house-balance').textContent = houseBalance.toFixed(1);
  // Add balance pulse animation on change
}
